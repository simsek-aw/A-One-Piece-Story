# Kopiere diese Datei nach ".env" und passe die Werte an.

# Welcher KI-Provider steuert den Spielleiter?
#   mock      -> deterministische Platzhalter-Engine (kein API-Key nötig)
#   gemini    -> echter Spielleiter über Google Gemini (kostenloses Kontingent
#                über https://aistudio.google.com/apikey — empfohlen ohne
#                OpenAI-Guthaben)
#   openai    -> echter Spielleiter über OpenAI (OPENAI_API_KEY nötig)
#   anthropic -> echter Spielleiter über Claude (ANTHROPIC_API_KEY nötig)
AI_PROVIDER=mock

# Nur für AI_PROVIDER=gemini:
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Nur für AI_PROVIDER=openai:
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Nur für AI_PROVIDER=anthropic:
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8

# Echte KI-Bild-Panels (Szenen, Schlüsselmomente, Profilbild) — optional,
# unabhängig vom Text-Spielleiter oben wählbar. Ist Gemini aktiv, hat es
# Vorrang vor OpenAI. Ohne eines der beiden bleiben die kostenlosen,
# sofortigen SVG-Panels aktiv.
GEMINI_IMAGES=0
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
OPENAI_IMAGES=0
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_QUALITY=low

# Server-Port
PORT=3000
